<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ghassen.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="hackathon"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            </intent-filter>
        </activity>
        <activity
            android:name=".Login"
            android:label="Authentification">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Register"
            android:label="S&apos;inscrire" />
        <activity
            android:name=".Caracteristique_evt"
            android:label="Détails de l&apos;évenement" />
        <activity
            android:name=".New_team"
            android:label="Créer une équipe" />
        <activity
            android:name=".Planning"
            android:label="Planning" />
        <activity android:name=".X_coding"
                  android:label="Détails de l&apos;évenement" ></activity>
    </application>

</manifest>